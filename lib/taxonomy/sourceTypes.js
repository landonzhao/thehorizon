/**
 * Source types — flat array export for Layer 5 prompt injection and validation.
 *
 * Canonical source of truth: lib/config/sourceTypes.js
 * This module re-exports the flat array and adds a minimal convenience alias.
 */

export {
  ALL_SOURCE_TYPES as SOURCE_TYPES,
  OPERATIONAL_TYPES,
  HORIZON_TYPES,
  SOURCE_TYPE_LABELS,
} from "../config/sourceTypes.js";

export { ALL_SOURCE_TYPES } from "../config/sourceTypes.js";
